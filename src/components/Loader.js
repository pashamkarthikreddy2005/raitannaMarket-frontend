import React from 'react';
import './Loader.css';

const Loader = () => {
    return (
        <div className="loader">
            <span className="letter-l">RM</span>
            <span className="dot"></span>
        </div>
    );
};

export default Loader;
